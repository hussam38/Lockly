import 'package:flutter/material.dart';

class UserSceneScreen extends StatelessWidget {
  const UserSceneScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center( 
      child: Text("User Scene"),
    );
  }
}