import 'package:flutter/material.dart';

class AdminSceneScreen extends StatelessWidget {
  const AdminSceneScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Admin Scene"),
    );
  }
}
